# v0.4.0 "Knowledge Autonomy" 发布 UAT 跟踪

> 创建日期: 2026-04-20
> 发布目标: v0.4.0
> 当前 develop HEAD: `7dc8718` (post-UAT reruns + evidence refresh)
> 负责人: Claude Code
>
> **用法**：每项 UAT 从 `TODO` 开始，执行时改为 `IN PROGRESS`，完成后改为 `PASS` / `FAIL` / `KNOWN ISSUE`，并在"证据"列填入 artifact/log 路径或 commit 哈希。

---

## 状态汇总

| 优先级 | 总数 | TODO | IN PROGRESS | PASS | FAIL / KNOWN ISSUE |
|--------|------|------|-------------|------|--------------------|
| P0 (release blocker) | 5 | 0 | 0 | 5 | 0 |
| P1 (建议发布前)      | 7 | 0 | 0 | 5 | 2 |
| P2 (发布后可补)      | 4 | 0 | 0 | 1 | 3 |
| **合计**             | **16** | **0** | **0** | **11** | **5** |

> **2026-04-20 修复轮次**（分支 `fix/v0.4-uat-blockers`）：P0/P1 的代码根因已修完，待在真机矩阵重跑 UAT 取 PASS 证据。
>
> | 修复 | 覆盖 UAT | 代码点 | 单元测试 |
> |------|----------|--------|----------|
> | F1 `total_cells=session.Total` | U2 | `internal/agent/exploration.go::summarizeTuningSession` | `TestSummarizeTuningSession_PartialMatrixReportsAttemptedTotal` |
> | F2 `DefaultInviteCode=""` + state=unregistered on prompt err | U6 offline-first | `internal/support/support.go`, `internal/support/bootstrap.go` | `TestBootstrap_NoInviteLeavesUnregistered` |
> | F3 严格 gate memory-fraction 合成 | U6/U10 llamacpp flag leak | `internal/knowledge/resolver.go::buildSyntheticConfig` | `TestBuildSyntheticConfig_NoVRAMLeakForEnginesWithoutDeclaredKnob` |
> | F4 `ResolvedConfig.EngineAssetName`，label 走新字段 | U9 type-alias label drift | `internal/knowledge/resolver.go` + `cmd/aima/tooldeps_deploy.go` | `TestResolveCarriesEngineAssetName` |
> | F5 `waitForReady` 检测 deploy-vanished，fail fast | U1/U3-deep/U5-attempt2 stuck-active | `internal/agent/exploration.go::waitForReady` | `TestWaitForReady_FailsFastWhenDeploymentDisappears`, `..._VanishAfterAliveCloses_Faster` |
> | F6 Central `timestamptz` 归一化 | U14 SQLite vs Postgres 字段格式 | `internal/central/store_postgres.go` | `TestPostgresCentralStore_ListScenariosTimestampsRFC3339UTC` |
> | F8 Docker restart-loop 视为 terminal failure；advisory plan 失败落 `rejected` 并回传更具体错误 | U5 | `internal/runtime/docker.go`, `internal/agent/explorer.go`, `internal/agent/exploration.go` | `TestDockerInspectToStatus`, `TestDockerStatusToPhase`, `TestWaitForReady_FailedPhaseIncludesDiagnostic`, `TestAdvisoryPlanStatus` |
> | U13 验收条件 | U13 version chicken-and-egg | 改验收：`版本 = series.txt+'-dev' ∨ v0.4.0 tag`；release 流程改为 tag → rebuild → smoke | — |
>
> **F7 撤回**（U10 onboarding 超时）：原计划用 `cold_start_s[1]*2+30s` 作为默认 deploy timeout，但 `cold_start_s` 语义是"引擎冷启动"而非"模型加载"，对 `llamacpp-universal.yaml` 的 `[3,10]` 会把默认从 10min 缩到 50s，反而让 U10 更糟。修正方案属于 YAML-first 范畴（应在 engine YAML 里补 `health_check.timeout_s` 或 `cold_start_s` 的真实上界），不在本代码轮次内做。

---

## 执行原则

遵循 `CLAUDE.md` "ALL COLLECT, THEN ANALYZE" 流程：

1. 本机交叉编译所有目标二进制
2. `scp` 到所有目标设备
3. 在所有相关设备上**并行**跑同一组测试命令
4. 等齐所有结果（不可达 = UNREACHABLE，不得跳过）
5. 用结果矩阵做统一分析
6. 一次性修改，避免"按下葫芦浮起瓢"
7. 每项 UAT 必须保留可追溯证据（日志、DB 快照、截图 GIF）到 `artifacts/uat/v0.4/<uat-id>/`

---

# P0 — Release Blocker

## U1 — Tune Warmup Readiness 跨设备验证

- **状态**: PASS
- **触发上下文**: `artifacts/uat/explorer/2026-04-17-explorer-rerun-v3-gb10-4t.md` 显示 Round 1 tune 5/5 configs 在 benchmark 前失败（2 次 `deploy result missing ready endpoint`、1 次 `benchmark has no throughput/QPS signal`、1 次 `context canceled`）。Commit `6fa91f6` "Honor warmup readiness and normalize served model labels" 本来修这个，但以"略微不同形态"复现。
- **前置条件**:
  - linux-1、gb10-4t、w7900d 上有 ≥1 个本地模型和兼容引擎
  - `aima serve` 运行中，Explorer 已升到 Tier ≥ 1
- **执行步骤**:
  1. 构造一个 narrow search space 的 tune 任务（如 `gpu_memory_utilization: [0.7, 0.8]`，只 2 个 cell）
  2. 在 linux-1（RTX 4090 × 2，最稳）上先跑
  3. 成功后复制到 gb10-4t 和 w7900d 跑
  4. 观察每个 cell 是否进入 benchmark 阶段
- **验收条件**:
  - 每台机器上至少 1 个 cell 能完成 benchmark（有 throughput > 0）
  - 日志里没有 "deploy result missing ready endpoint" 的循环
  - `experiment-facts.md` 写入成功 cell 数据
- **建议设备**: linux-1 → gb10-4t → w7900d
- **证据**:
  - `artifacts/uat/v0.4/u1/README.md`
  - `artifacts/uat/v0.4/u1-gb10-4t-rerun/README.md`
  - `artifacts/uat/v0.4/u1-linux-1-rerun/README.md`
  - `artifacts/uat/v0.4/u1-w7900d-rerun/README.md`
  - `artifacts/uat/v0.4/u1/serve.log`
  - `artifacts/uat/v0.4/u1/status/001.json` → `status/031.json`
  - `artifacts/uat/v0.4/u1/aima.db.snapshot`
  - `artifacts/uat/v0.4/u1-linux-1-rerun/11-tuning-polls-rerun3.txt`
  - `artifacts/uat/v0.4/u1-linux-1-rerun/14-db-counts-rerun3.txt`
  - `artifacts/uat/v0.4/u1-linux-1-rerun/16-host-probe.txt`
  - `artifacts/uat/v0.4/u1-linux-1-rerun/23-tuning-results-final.json`
  - `artifacts/uat/v0.4/u1-linux-1-rerun/24-db-counts-final.txt`
  - `artifacts/uat/v0.4/u1-gb10-4t-rerun/06-tuning-results.json`
  - `artifacts/uat/v0.4/u1-gb10-4t-rerun/07-db-counts.txt`
  - `artifacts/uat/v0.4/u1-gb10-4t-rerun/11-tuning-session-db.txt`
  - `artifacts/uat/v0.4/u1-w7900d-rerun/13-tuning-results-patched.json`
  - `artifacts/uat/v0.4/u1-w7900d-rerun/18-tuning-session-db.txt`
  - 2026-04-20 在 `gb10-4T` (`qujing@100.91.39.109`) 实测 `qwen3.5-9b + vllm-nightly + gmu=[0.25,0.30]`，run `2103c0f57a5baea4`
  - 结果：11m25s 内未复现旧的 `deploy result missing ready endpoint` 快速失败，但首个 cell 始终停在 container compatibility auto-repair / pre-listen 阶段，`progress=0/2`、无 benchmark、无 `throughput > 0`
  - 2026-04-21 在 `linux-1` (`cjwx@linux-1`) 用隔离目录 `~/aima-uat-rerun/u1-fix/data` 补跑 `qwen3-4b + vllm`；第一次因 isolated DB 没有模型事实而直接失败，补导入本地模型后又分别尝试 `gmu=[0.10,0.12]` 与 `gmu=[0.07,0.08]`
  - 新 rerun 已不再卡 `deploy result missing ready endpoint` / stuck-active；`11-tuning-polls-rerun3.txt` 显示 session 从 `0/2` 推进到 `1/2`，最终以 `failed progress=2/2` 收敛
  - `15-serve-tail-rerun3-short.txt` 记录当前剩余问题是现场资源：两格都以 `deployment failed: GPU memory insufficient` 终止，其中一格还带出精确诊断 `Free memory on device cuda:0 (5.6/47.37 GiB) ...`
  - `16-host-probe.txt` 记录当时 `linux-1` 两张 4090 仅剩 `6124 MiB / 6943 MiB` 空闲显存
  - 在显式停掉现场 `qwen3-4b-vllm` 与 `Qwen3.5-122B-A10B sglang-kt` 后，又用全新隔离目录 `~/aima-uat-rerun/u1-final/data` 补跑一次；`17-stop-current-models.txt` 记录 cleanup 后两张 4090 都恢复到 `48508 MiB` 空闲显存
  - 这次 `linux-1` final rerun 的 tuning session `285ee8967d35cf2d` 已 `completed 2/2`：第一格 `gmu=0.20` 因 KV cache 不足自然失败，第二格 `gmu=0.25` 成功完成 benchmark，`throughput_tps=102.5510`、`ttft_p95_ms=21.322`、`benchmark_id=26dbf541072e8629`
  - `24-db-counts-final.txt` 证明该隔离 DB 已持久化 `benchmarks = 1`、`configs = 1`，且 `best_config.gpu_memory_utilization = 0.25`
  - 2026-04-21 在 `w7900d` (`root@36.151.243.68:21985`) 用隔离目录 `/root/aima-uat-rerun/u1-w7900d/data` 补跑 `Qwen3-30B-A3B-Q4_K_M + llamacpp-rocm-rdna3 + ctx_size=[2048,4096]`
  - 首次尝试把 `llamacpp-rocm-rdna3` 打到了一个更具体的 asset 问题：`ggml-org/llama.cpp:full-rocm` 实际把 `llama-server` 放在 `/app/llama-server`，旧配置里的裸 `llama-server` 会直接报 `exec: "llama-server": executable file not found in $PATH`
  - 用当前 workspace 对 `catalog/engines/llamacpp-rocm-rdna3.yaml` 做最小修补后重跑，同一台机器上的 tuning session `531cc06928183275` 已 `completed 2/2`，两格都落下真实 benchmark：`throughput_tps=62.8267 / 62.9274`
  - `16-db-counts-patched.txt` 与 `18-tuning-session-db.txt` 证明隔离 DB 已持久化 `benchmarks = 2`、`configs = 2`，最佳 config 为 `ctx_size=4096`
  - 2026-04-21 用当前 `HEAD=7dc8718` 在 `gb10-4T` (`qujing@100.91.39.109`) 起全新隔离 rerun：`GLM-4.1V-9B-Thinking + vllm-nightly`，参数压到 `gpu_memory_utilization=[0.85] × max_model_len=[384,512]`
  - 这次 tuning session `29741d52116fbc06` 已 `completed 2/2`。两格都落下真实 benchmark：`throughput_tps=10.1357 / 10.1330`，对应 `benchmark_id=7b47a884740e344a / bf4fb17e4cb30067`
  - `07-db-counts.txt` 证明该隔离 DB 已持久化 `benchmarks = 2`、`configs = 2`、`sessions = 1`；`11-tuning-session-db.txt` 记录最终 `status=completed`、`best_config.max_model_len=384`
  - 这轮 `gb10-4T` 证据里没有再出现旧的 `deploy result missing ready endpoint` 循环；最终 `09-serve-tail-final.txt` 还能看到 `tuning: deployed best config`
  - 结论：U1 的原始 warmup/readiness blocker 现在已在 `linux-1`、`gb10-4T`、`w7900d` 三类目标机上拿到成功 benchmark 证据，本项转为 `PASS`。2026-04-20 的 `qwen3.5-9b` repair-init 现场保留作资产级历史对照，但不再代表当前 verdict

---

## U2 — Explorer Partial-Preserve Cells 分支验证

- **状态**: PASS
- **触发上下文**: rerun v3 的 author 自述："round 1 tune 5/5 全部在 benchmark 前失败，所以此分支无数据可保留。下一轮需要刻意跑一次能部分成功的 tune 来补齐证据。" — Fix #1 的逻辑分支从未被执行过。
- **前置条件**: U1 至少有一台机器能跑完部分 cell
- **执行步骤**:
  1. 设计一个 tune search space 使得约 50% cell 会成功、50% 会失败（例：一个合理 gmu 值 + 一个明显过高的值触发 OOM）
  2. 运行 tune
  3. 检查：
     - `experiment-facts.md` 保留成功 cell 的完整 benchmark 数据
     - 失败 cell 被标记 `skipped_fail` 而不是丢整轮
     - `summary.md` 正确汇总（不是 "all failed" 也不是 "all passed"）
- **验收条件**:
  - DB 里只有成功 cell 的 `benchmark_results` 记录（零吞吐不被 promote）
  - Agent 下一轮 plan 能基于部分成功结果继续提案
- **建议设备**: linux-1（最稳）
- **证据**:
  - `artifacts/uat/v0.4/u2-gb10-4t/README.md`
  - `artifacts/uat/v0.4/u2-gb10-4t/rerun-u2fix-qwen/03-explore-start.json`
  - `artifacts/uat/v0.4/u2-gb10-4t/rerun-u2fix-qwen/04-status-polls.jsonl`
  - `artifacts/uat/v0.4/u2-gb10-4t/rerun-u2fix-qwen/05-run-result.json`
  - `artifacts/uat/v0.4/u2-gb10-4t/rerun-u2fix-qwen/06-db-counts.txt`
  - `artifacts/uat/v0.4/u2-gb10-4t/rerun-u2fix-qwen/07-serve-tail.txt`
  - `artifacts/uat/v0.4/u2-gb10-4t/rerun-u2fix-qwen/10-perf-observation.json`
  - `artifacts/uat/v0.4/u2-gb10-4t/rerun-u2fix-qwen/11-config-status.txt`
  - 2026-04-21 在同一台空闲 `gb10-4T` (`qujing@100.91.39.109`) 改用 patched build `af9ba09` 重跑，但把验证组合换成已在 U9 证实 ready 的 `qwen3.5-9b + vllm-nightly`，避免旧 Gemma 组合把大部分时间花在与 partial-preserve 无关的首格冷启动上
  - 这次显式构造 2-cell tune：`dtype=[bfloat16, definitely-not-real]`，并把 benchmark profile 压到单点 `concurrency=1 / input_tokens=128 / max_tokens=32 / num_requests=1`
  - 第一格 `dtype=bfloat16` 成功完成 benchmark；`serve.log` 记录代理实际转发了 `/v1/chat/completions`，最终写入真实结果：`benchmark_id=8928d721e8010732`、`config_id=a76d78bb3e5381a7`、`throughput_tps=12.307901395267972`
  - 第二格 `dtype=definitely-not-real` 自然在 deploy 阶段失败，`serve.log` 明确记录：`deploy run qwen3.5-9b: deployment failed: vllm serve: error: argument --dtype: invalid choice: 'definitely-not-real'`
  - tune run 最终正常收敛为 `status=completed`，不再像旧现场那样长期卡在 `running`
  - 最终 `summary_json` 已正确区分“尝试总格数”和“成功格数”：`total_cells=2`、`success_cells=1`；内嵌 `tuning_session` 同时是 `status=completed`、`progress=2`、`total=2`、`results_len=1`
  - DB 里仍只有 1 条 `benchmark_results` 与 1 条 `configurations`，证明失败格没有被 promote
  - 成功格已进入后续 planner 可消费状态：`configurations.status=golden`，且隔离数据目录里新增 `observations/models/qwen3.5-9b-perf.json`

---

## U3 — Integration Gates Scenario 1: New Device Onboarding E2E

- **状态**: PASS
- **触发上下文**: `docs/superpowers/plans/2026-04-07-v0.4-integration-gates.md` §3 + spec §5.3"新设备上线"流程，release stop condition。
- **前置条件**:
  - 一台从未连接过 Central 的设备（或重置 `~/.aima` 和设备 identity）
  - `CENTRAL_LLM_API_KEY` 可用
  - 本 repo CHANGELOG 同时在 `aima-central-knowledge` 有对应契约版本部署到生产或测试环境
- **执行步骤**:
  1. 设备：`aima --invite-code <CODE> device register`
  2. 设备：`aima device status` → 确认 `registered`
  3. 设备：`aima serve` + Explorer 启动
  4. 设备：`aima knowledge sync --push` → 首次推送设备信息
  5. Central：确认 `UpsertDevice()` 生效、`PostIngest` 触发 gap 扫描、生成 advisory
  6. 设备：等待 Explorer 事件 `central.advisory` 或手动 `aima knowledge sync --pull`
  7. 设备：Explorer 验证 advisory 并 `feedback`
  8. Central：确认 advisory `status` 变为 `validated` 或 `rejected`，不再是 `pending/delivered`
- **验收条件**:
  - advisory 在 central 存在
  - edge validation 至少落下一条 benchmark / configuration artifact
  - 最终 advisory lifecycle ≠ `pending`
- **建议设备**: aibook 或 metax-n260（未参与过 v0.4 UAT 的新机器）
- **证据**:
  - `artifacts/uat/v0.4/u3-gb10-4t/README.md`
  - `artifacts/uat/v0.4/u3-gb10-4t/04-mock-aima-service-check.json`
  - `artifacts/uat/v0.4/u3-gb10-4t/05-central-stats-after-push.json`
  - `artifacts/uat/v0.4/u3-gb10-4t/06-central-advisories-pending.json`
  - `artifacts/uat/v0.4/u3-gb10-4t/12-central-advisories-manual-insert.json`
  - `artifacts/uat/v0.4/u3-gb10-4t/14-central-advisories-after-glm-insert.json`
  - `artifacts/uat/v0.4/u3-gb10-4t/15-central-advisories-after-glm-pull.json`
  - `artifacts/uat/v0.4/u3-gb10-4t/16-central-advisories-final-window.json`
  - `artifacts/uat/v0.4/u3-gb10-4t/17-central-advisory-summary.txt`
  - `artifacts/uat/v0.4/u3-gb10-4t/central.db`
  - `artifacts/uat/v0.4/u3-gb10-4t/rerun-u3fast-local/19-sync-pull-direct-mcp.json`
  - `artifacts/uat/v0.4/u3-gb10-4t/rerun-u3fast-local/21-polls-fast2.txt`
  - `artifacts/uat/v0.4/u3-gb10-4t/rerun-u3fast-local/22-central-advisories-final-fast2.json`
  - `artifacts/uat/v0.4/u3-gb10-4t/rerun-u3fast-local/23-serve-tail-final-fast2.txt`
  - `artifacts/uat/v0.4/u3-gb10-4t/rerun-u3fast-local/24-edge-counts-final-fast2.txt`
  - `artifacts/uat/v0.4/u3-gb10-4t/rerun-u3fast-local/25-fast2-final-status.txt`
  - 2026-04-20 因 `aibook` / `metax-n260` 仍不可达，仍由空闲的 `gb10-4T` (`qujing@100.91.39.109`) 用隔离 `AIMA_DATA_DIR` 扮演“新设备”；`dev-mac` 本机起 repo 自带 `cmd/central` 与 mock `aima-service` / mock OpenAI，并通过 SSH reverse tunnel 暴露给远端
  - 初始 onboarding 链路仍保持通过：新设备注册成功、首次 `knowledge sync --push` 把 edge 数据写入 Central、浅层 advisory 仍可走到 `rejected`
  - 2026-04-21 新增 fast rerun：改为 direct MCP `central.sync pull` 把深层 advisory 直接送进 live Explorer，避免旧 wrapper 路径把 advisory 标成 `delivered` 但没真正触发远端 Explorer
  - fast rerun 的目标 advisory 为 `adv_u3_glm_validate_fast2`，组合 `GLM-4.1V-9B-Thinking + vllm-nightly`，并显式加 `max_model_len=384` 把 validate 裁成单格 benchmark
  - `23-serve-tail-final-fast2.txt` 记录深层 validate 真正完成：deploy `05:07:14Z`，service ready `05:11:32Z`，benchmark `1/1` 于 `05:14:27Z` 成功结束，结果 `throughput_tps=10.28502283151757`、`ttft_p95_ms=102.0084`
  - `24-edge-counts-final-fast2.txt` 显示 edge 端已落下 `benchmarks=1`、`configs=1`，且 plan `advisory-adv_u3_glm_validate_fast2` 终态 `completed`
  - `25-fast2-final-status.txt` 与 `22-central-advisories-final-fast2.json` 显示 Central 已把该 advisory 标成 `status=validated`、`accepted=1`，反馈为 `validated: 10.3 tok/s, TTFT P95 102ms`
  - 结论：U3 现在满足全部验收条件，旧的 2026-04-20 stalled evidence 保留作历史对照，但当前 verdict 已切换为 `PASS`

---

## U4 — Integration Gates Scenario 2: Tier 2 → Tier 1 降级

- **状态**: PASS
- **触发上下文**: spec §6.2 + integration-gates.md §3，release stop condition。
- **前置条件**:
  - linux-1 上 Explorer 处于 Tier 2（连接云端 LLM，如 Kimi）
  - 正在执行一个包含多 task 的 exploration plan
- **执行步骤**:
  1. `aima agent status` 确认 Tier 2
  2. 执行 `aima explorer trigger`，启动一次 exploration
  3. 在 LLM 调用进行中断开网络（或临时拦截 LLM endpoint）
  4. 触发一个新的 exploration event
  5. 观察 Explorer 日志
  6. 恢复网络
  7. 等待 5 分钟重试周期
  8. 再触发一次 exploration
- **验收条件**:
  - 日志出现 "LLM unavailable, degrading to Tier 1" 或同语义消息
  - 断连期间 RulePlanner 继续执行确定性任务
  - 已完成工作（benchmark_results）未丢
  - 不需要重启进程
  - 恢复后 Tier 自动回到 2
- **建议设备**: linux-1
- **证据**:
  - `artifacts/uat/v0.4/u4-gb10-4t/README.md`
  - `artifacts/uat/v0.4/u4-gb10-4t/14-15-status-reset.txt`
  - `artifacts/uat/v0.4/u4-gb10-4t/21-22-status-after-invalid-endpoint.txt`
  - `artifacts/uat/v0.4/u4-gb10-4t/26-log-after-long-wait.txt`
  - `artifacts/uat/v0.4/u4-gb10-4t/27-status-after-long-wait.json`
  - `artifacts/uat/v0.4/u4-gb10-4t/29-30-status-after-restore.txt`
  - `artifacts/uat/v0.4/u4-gb10-4t/33-status-and-db-after-first-cell.txt`
  - `artifacts/uat/v0.4/u4-gb10-4t/34-log-after-first-cell.txt`
  - `artifacts/uat/v0.4/u4-gb10-4t/36-remote-cleanup.txt`
  - 2026-04-20 因 `linux-1` 上有共享 `aima-serve` / 推理服务，本轮改在空闲的 `gb10-4T` (`qujing@100.91.39.109`) 用隔离 `AIMA_DATA_DIR` 与隔离端口 `6284/9184` 验证，不干扰现网服务
  - 基线确认：fault injection 前，远端 `agent.status` 为 `agent_available=true`，`explorer.status` 顶层 `tier=2`
  - 在同一轮 Tier 2 `phase=plan` 中途，把 `llm.endpoint` 从 `https://api.kimi.com/coding/v1` 热更新到 `http://127.0.0.1:1/v1`；随后远端 `agent.status` 变为 `agent_available=false`
  - `serve.log` 明确出现同语义降级日志：`explorer: tier 2 planner failed` 与 `explorer: degrading to Tier 1 planner`；同时 `explorer.status` 进入 `active_plan.Tier=1`、`Reasoning="rule-based (degraded from Tier 2)"`
  - 同一进程内没有重启 `aima serve`；degraded plan 继续执行确定性 `validate` 任务，实际部署 `GLM-4.1V-9B-Thinking / vllm-nightly`
  - 恢复 `llm.endpoint` 后，远端 `agent.status` 再次回到 `agent_available=true`；同一轮 degraded validate 继续推进并真正进入 benchmark matrix
  - 第一个 benchmark cell 于 `11:33:52Z` 成功结束，隔离 DB 中写入 `benchmark_results=1`、`configurations=1`，最新结果为 `benchmark_id=14807b1bdd291eef`、`config_id=ea87b025050e3bf5`、`throughput_tps=10.1059698391381`
  - 观察备注：顶层 `explorer.status.tier` 字段未临时掉到 `1`，实际降级体现在日志和 `active_plan.Tier=1`；功能上已满足“Tier 2 失败后自动降到 Tier 1 并继续执行”的 release gate

---

## U5 — Integration Gates Scenario 3: Advisory Rejection Path

- **状态**: PASS
- **触发上下文**: spec §5.2 + integration-gates.md §3，release stop condition。
- **前置条件**:
  - Central 可直接 SQL/API 注入 advisory（开发环境）
  - linux-1 设备已注册、Explorer 运行中
- **执行步骤**:
  1. Central：手动插入一条明显不可执行的 advisory（例：目标硬件 RTX 4090 × 2，推荐配置运行 Qwen3.5-122B-A10B 但 TP=1，显然 OOM）
  2. Edge：`aima knowledge sync --pull` 拉取
  3. Edge：Explorer 自动验证该 advisory
  4. Edge：benchmark 失败（OOM 或 crash）
  5. Edge：feedback 发送到 central
  6. Central：确认 advisory status → `rejected`，保存 `rejection_reason`
  7. Edge：再次 `sync --pull`
- **验收条件**:
  - `rejection_reason` 在 central advisory 记录可见
  - 同一 advisory 不会再作为 `pending` 被下发
  - edge 本地 exploration_plans 里该任务被标记 `rejected`
- **建议设备**: linux-1
- **证据**:
  - `artifacts/uat/v0.4/u5-gb10-4t/README.md`
  - `artifacts/uat/v0.4/u5-gb10-4t/rerun-u5fix-local/05-central-sync-pull.json`
  - `artifacts/uat/v0.4/u5-gb10-4t/rerun-u5fix-local/06-plan-run-polls.jsonl`
  - `artifacts/uat/v0.4/u5-gb10-4t/rerun-u5fix-local/07-central-polls.jsonl`
  - `artifacts/uat/v0.4/u5-gb10-4t/rerun-u5fix-local/08-final-edge-state.json`
  - `artifacts/uat/v0.4/u5-gb10-4t/rerun-u5fix-local/10-central-state-final.json`
  - `artifacts/uat/v0.4/u5-gb10-4t/rerun-u5fix-local/11-central-sync-pull-after-reject.json`
  - 2026-04-21 延续前一轮 `gb10-4T` 隔离验证，但改用 patched build `af9ba09` 重跑，并把 edge 侧隔离目录/端口切到 `~/aima-uat-rerun/u5-fix/data`、`6288/9188`
  - mock Central 仍由 `dev-mac` 本地起在 `127.0.0.1:18086`，通过 SSH reverse tunnel 映射到远端 `127.0.0.1:18087`
  - 本轮仍使用会真正创建 validation plan 的深层 advisory：`adv-u5-reject-u5fix`，目标 `GLM-4.1V-9B-Thinking + vllm-nightly`，坏参数 `dtype=definitely-not-real`
  - 首次 `central.sync pull` 返回 `advisories.count=1`、`published_events=1`，证明 advisory 事件已进入 Explorer
  - edge 最终收敛为：`exploration_plans.id=advisory-adv-u5-reject-u5fix`、`status=rejected`、`progress=1/1`；对应 `exploration_runs.status=failed`
  - run `error` 与 Central `feedback` 都带出具体诊断：`deployment ... entered terminal phase "failed": vllm serve: error: argument --dtype: invalid choice: 'definitely-not-real'`
  - mock Central 最终 advisory 记录为 `status=rejected`、`accepted=false`、`validated_at=2026-04-21T03:23:08Z`
  - 第二次 `central.sync pull` 返回 `advisories.count=0`、`published_events=0`，说明同一 advisory 不会再以 `pending` 被重复下发

---

# P1 — 建议发布前验证

## U6 — aima-service 首启动 invite_code 缺失路径

- **状态**: PASS
- **触发上下文**: v0.4 新增 Phase 1 功能，spec 声称 offline-first + 非阻塞注册，但未在真机验证"无 invite_code 时行为正确"。
- **前置条件**: 一台重置过 `~/.aima` 的设备
- **执行步骤**:
  1. 不设置 `AIMA_INVITE_CODE`、不传 `--invite-code`
  2. 启动 `aima serve`
  3. 观察 30 秒
  4. `aima device status` → 应显示 `unregistered` 或 `pending`
  5. 在没有 identity 的情况下尝试正常本地推理（`aima deploy <model>`, `aima benchmark run ...`）
  6. 设置 `AIMA_INVITE_CODE` 后重启，或运行 `aima device register --invite-code <CODE>`
  7. 确认 `device.id`、`device.token`、`device.recovery_code` 三个 canonical key 都写入
  8. 确认 `support.state.device_id` 等"私有"key 也写入（双写）
- **验收条件**:
  - 无 invite_code 时：本地推理正常；Central 调用被 `RequireRegistered` 优雅拒绝（非 crash、非 silent drop）
  - 配置 invite_code 后：`aima device status` 从 `unregistered` 变为 `registered`
  - Canonical keys 和 private keys 值一致
- **建议设备**: test-win 或 gb10-4t（重置 `~/.aima` 后测）
- **证据**:
  - `artifacts/uat/v0.4/u6-light-salt/README.md`
  - `artifacts/uat/v0.4/u6-light-salt/10-rerun-af9ba09.json`
  - `artifacts/uat/v0.4/u6-light-salt/03-device-status-before.txt`
  - `artifacts/uat/v0.4/u6-light-salt/04-sync-push-before-serve.txt`
  - `artifacts/uat/v0.4/u6-light-salt/05-deploy.txt`
  - `artifacts/uat/v0.4/u6-light-salt/06-deploy-list-after-fail.txt`
  - `artifacts/uat/v0.4/u6-light-salt/08-identity-mirror-check.txt`
  - `artifacts/uat/v0.4/u6-light-salt/09-device-status-after-serve.txt`
  - 2026-04-21 在 `light-salt` (`jguan@100.114.25.35`) 用当前 `HEAD=af9ba09` 重跑隔离目录 `C:\Users\jguan\aima-uat-rerun\u6-pass\data`
  - 结果 1：`device status` 初始为 `registered=false` / `registration_state=unregistered`；`knowledge sync --push` 继续被 `device not registered with aima-service` 优雅拒绝
  - 结果 2：Windows native `llamacpp` 路径已修通。`deploy Qwen3-0.6B-Q8_0 --engine llamacpp` 返回 `0`，deploy config 只包含 `ctx_size` / `n_gpu_layers` / `port`，不再泄漏 `--gpu-memory-utilization`
  - 结果 3：部署轮询第 1 次为 `startup_phase=loading_model`、第 2 次达到 `ready=true`；最终 `deploy list` 为 `runtime=native`、`engine=llamacpp-universal`、`ready=true`
  - 结果 4：`device register --invite-code U6-INVITE-20260421` 成功，`device.id` / `device.token` / `device.recovery_code` 与 `support.state.*` 镜像键一致

---

## U7 — MCP-Initiated Tune 脱离 HTTP 请求 Context

- **状态**: PASS
- **触发上下文**: commit `74b45ac` "Detach MCP-initiated tune from HTTP request context"。修复落地但未端到端验证。
- **前置条件**: linux-1 上 Explorer 和 MCP server 运行
- **执行步骤**:
  1. 通过 MCP 发送 `tuning.start`（action=start），设计一个会跑 10 分钟的 tune
  2. 立刻检查 HTTP 响应是否已返回（非阻塞）
  3. 客户端断开 HTTP 连接
  4. 等待 2 分钟后，通过新连接发送 `tuning.status` → 确认 tune 仍 `running`
  5. 等到 tune 跑完后 `tuning.results` → 确认有结果
- **验收条件**:
  - HTTP 响应返回时 tune 尚未完成
  - HTTP 断开不影响 tune 进程
  - tune 完成后结果正确写入 DB
- **建议设备**: linux-1
- **证据**:
  - `artifacts/uat/v0.4/u7-gb10/README.md`
  - `artifacts/uat/v0.4/u7-gb10/01-tuning-start.json`
  - `artifacts/uat/v0.4/u7-gb10/status/002-after-2min-mcp.json`
  - `artifacts/uat/v0.4/u7-gb10/status/003.json` → `status/006.json`
  - `artifacts/uat/v0.4/u7-gb10/03-docker-after-2min.txt`
  - `artifacts/uat/v0.4/u7-gb10/04-tuning-results.json`
  - `artifacts/uat/v0.4/u7-gb10/05-serve-final.log`
  - 2026-04-20 在 `gb10-4T` (`qujing@100.91.39.109`) 用隔离 MCP 服务直发 `tuning.start`，session `87cce8429c91e4b7`
  - 初始 HTTP 响应在约 `30ms` 内返回，而 session 立即进入 `running`
  - 2 分钟后用新 MCP 连接查询，同一 session 仍为 `running progress=0/1`，同时 Gemma 容器仍处于 `health: starting`
  - 最终 session `completed`，`tuning.results` 返回真实 benchmark：`throughput_tps=20.2735`，并包含 `benchmark_id=c55bc30e74786dd3`、`config_id=d067bf14f2abc765`

---

## U8 — Edge HTTP Timeout 600s for LLM Reasoning

- **状态**: PASS
- **触发上下文**: commit `b2dc0ae` "extend edge HTTP timeout to 600s for LLM reasoning endpoints"。
- **前置条件**: 可用的 Kimi reasoning 或其他慢 LLM 端点（响应时间 100+ 秒）
- **执行步骤**:
  1. 配置 `central.endpoint` 或 `agent.llm.endpoint` 指向慢 LLM
  2. 发送一个复杂查询（确保触发 reasoning，响应 > 120 秒）
  3. 观察是否提前被 edge timeout 截断
- **验收条件**:
  - 响应 300 秒级别仍成功返回
  - 日志无 `context deadline exceeded` 错误
- **建议设备**: linux-1 或 dev-mac
- **证据**:
  - `artifacts/uat/v0.4/u8-dev-mac/README.md`
  - `artifacts/uat/v0.4/u8-dev-mac/mock_central.py`
  - `artifacts/uat/v0.4/u8-dev-mac/00-mock-central.log`
  - `artifacts/uat/v0.4/u8-dev-mac/01-knowledge-advise.json`
  - 2026-04-20 在 `dev-mac` 用隔离 `AIMA_DATA_DIR` + 本地 slow Central mock 实测 `knowledge advise qwen3-8b --engine vllm --intent low-latency`
  - mock Central 于 `2026-04-20T09:06:19Z` 收到 `/api/v1/advise` 请求，固定延迟 `130s` 后在 `2026-04-20T09:08:29Z` 返回 `200`
  - edge CLI 实际耗时 `130.192s`，`returncode=0`，stdout 为规范化 advisory JSON，stderr 无 `context deadline exceeded`
  - 这是对 `RequestAdvise` 所走 `syncHTTPClient := &http.Client{Timeout: 600 * time.Second}` 路径的直接验证

---

## U9 — Engine Health_Check Timeout + vllm-nightly Warmup

- **状态**: PASS
- **触发上下文**: commit `5657269` "Honor engine health_check timeout and wire vllm-nightly warmup"。
- **前置条件**: gb10 或 gb10-4t（本地已缓存 vllm-nightly image）
- **执行步骤**:
  1. 在 docker runtime 上 deploy 一个 vllm-nightly 模型
  2. 观察 warmup 阶段是否遵守配置的 timeout（如 300s）
  3. 构造一个 engine health_check 必然失败的场景（如错误的启动参数）
  4. 验证到达 timeout 后立即失败，不无限等
- **验收条件**:
  - 正常场景：warmup 在 timeout 内完成或失败
  - 异常场景：不卡死
- **建议设备**: gb10 + gb10-4t
- **证据**:
  - `artifacts/uat/v0.4/u9-gb10-4t/README.md`
  - `artifacts/uat/v0.4/u9-gb10-4t/rerun-af9ba09/02-status-initial.txt`
  - `artifacts/uat/v0.4/u9-gb10-4t/rerun-af9ba09/03-status-model-init.txt`
  - `artifacts/uat/v0.4/u9-gb10-4t/rerun-af9ba09/04-status-loading-shards.txt`
  - `artifacts/uat/v0.4/u9-gb10-4t/rerun-af9ba09/05-status-ready.txt`
  - `artifacts/uat/v0.4/u9-gb10-4t/rerun-af9ba09/06-health.txt`
  - `artifacts/uat/v0.4/u9-gb10-4t/rerun-af9ba09/08-models-body.json`
  - `artifacts/uat/v0.4/u9-gb10-4t/rerun-af9ba09/09-labels.json`
  - `artifacts/uat/v0.4/u9-gb10-4t/07-type-engine-false-ready.txt`
  - `artifacts/uat/v0.4/u9-gb10-4t/status/normal-exact/029.txt` → `038.txt`
  - `artifacts/uat/v0.4/u9-gb10-4t/11-normal-exact-docker-logs-3.txt`
  - `artifacts/uat/v0.4/u9-gb10-4t/14-fail-run.stdout`
  - `artifacts/uat/v0.4/u9-gb10-4t/16-fail-correct-health.txt`
  - `artifacts/uat/v0.4/u9-gb10-4t/17-fail-bad-health.txt`
  - `artifacts/uat/v0.4/u9-gb10-4t/18-fail-docker-logs.txt`
  - 2026-04-21 在 `gb10-4T` (`qujing@100.91.39.109`) 用当前 `HEAD=af9ba09` 重跑原本失败的 alias 路径，隔离目录 `~/aima-uat-rerun/u9/data`
  - 结果 1：`aima deploy qwen3.5-9b --engine vllm-nightly --config port=18083` 现在把容器 label 写成了 `aima.dev/engine=vllm-nightly-blackwell`，首个可见 `deploy status` 即为 `ready=false`
  - 结果 2：alias 路径状态按预期推进 `initializing -> model_init -> loading_shards -> ready`，未再出现“repair-init 期间秒报 ready=true”
  - 结果 3：最终 `deploy status qwen3-5-9b-vllm-nightly` 返回 `ready=true`，`/health` 返回 `200`，`/v1/models` 返回 `200` 且模型 ID 为 `qwen3.5-9b`
  - 结果 4：2026-04-20 已留存的 exact-asset 正常路径与 bad-health timeout 路径证据仍成立，因此本项验收条件已满足

---

## U10 — Onboarding 向导跨设备 Smoke

- **状态**: KNOWN ISSUE
- **触发上下文**: v0.4 新功能，原 UAT 覆盖 linux-1、gb10-4t 为主，其它设备未验证。
- **前置条件**: 每台机器已装 AIMA v0.4 二进制
- **执行步骤**（每台设备）:
  1. `aima onboarding start`（或 Web UI `/onboarding`）
  2. 走完：硬件检测 → 扫描本地资产 → 推荐 top 模型 → 选择并部署
  3. 部署成功后通过 `aima deploy list` 确认
- **验收条件**:
  - 每台设备整个流程不报错
  - 推荐分数合理（LLM > ASR/TTS；首次使用优先推荐可信的小/中模型，过大 native 模型不应压过可稳定启动的模型）
  - 部署后可用 `curl` 打到 inference endpoint
- **建议设备**: test-win、amd395、aibook、w7900d、metax-n260（五台）
- **证据**:
  - `artifacts/uat/v0.4/u10-light-salt/README.md`
  - `artifacts/uat/v0.4/u10-light-salt/01-status.json`
  - `artifacts/uat/v0.4/u10-light-salt/02-scan.json`
  - `artifacts/uat/v0.4/u10-light-salt/03-recommend.json`
  - `artifacts/uat/v0.4/u10-light-salt/06-deploy.stderr`
  - `artifacts/uat/v0.4/u10-light-salt/08-deploy-status.txt`
  - `artifacts/uat/v0.4/u10-light-salt/11-deploy-logs.txt`
  - `artifacts/uat/v0.4/u10-light-salt/17-deploy-small.stderr`
  - 2026-04-24 develop 修复前半段产品面：`aima onboarding start` 已成为真实 CLI 子命令，`aima onboarding` 无参数也会执行只读向导；native-only `docker/k3s=skipped` 不再误报 `needs_init=true`；小内存 native 推荐对过大模型加 first-run 风险惩罚。本项 Windows 真实部署仍需重跑验证后才能从 KNOWN ISSUE 改为 PASS。
  - 2026-04-20 在 `light-salt` (`jguan@100.114.25.35`) 用隔离 `AIMA_DATA_DIR` 走 onboarding 实流；当时 CLI 没有单独的 `onboarding start` 子命令，因此按同一代码路径执行了 `onboarding status -> scan -> recommend -> deploy`
  - 结果 1：前半段通过。`status` 正确识别 `nvidia-rtx4060-x86` / Ada / 8GB；`scan` 找到本地模型并优雅处理未注册设备；`recommend` 把 LLM 排在前面，并优先推荐更大的可运行模型（top1 为 `qwen3-30b-a3b`）
  - 结果 2：用 top1 `qwen3-30b-a3b + llamacpp` 部署时，Windows CUDA `llama.cpp` 二进制下载成功，`llama-server.exe` 真实启动并持续加载模型，但 onboarding 在 60s 后返回 `deployment started but not ready within 1m0s`；后查 `deploy status` 仍为 `startup_phase=loading_model`，`/health` 与 `/v1/models` 都是 `503`
  - 结果 3：改用更小的本地 GGUF `Qwen3-0.6B-Q8_0 + llamacpp` 作为对照后，native Windows 路径仍未通过：生成命令再次带出 `--gpu-memory-utilization 0.5`，最终失败于 `start ... via schtasks: discover PID after schtasks launch`
  - 结果 4：本轮其余建议设备未完成覆盖：`amd395`、`w7900d` 因已有共享服务未动；`aibook`、`metax-n260` 在本轮 SSH 超时

---

## U11 — 多模态 Benchmark E2E

- **状态**: KNOWN ISSUE
- **触发上下文**: v0.4 新增 Requester 接口 + 5 种模态 adapter + V14 migration。
- **前置条件**:
  - w7900d 上已部署 Flux2（T2I）和 LTX-2.3（T2V）
  - aibook 上已部署 TTS/ASR（Mooer/FunASR）
- **执行步骤**:
  1. Chat: `aima benchmark matrix --modality chat --model qwen3-30b-a3b`（任意有 chat 部署的机器）
  2. TTS: `aima benchmark matrix --modality tts --model mooer-tts-zh`（aibook）
  3. ASR: `aima benchmark matrix --modality asr --model funasr`（aibook）
  4. T2I: `aima benchmark matrix --modality t2i --model flux2-dev`（w7900d）
  5. T2V: `aima benchmark matrix --modality t2v --model ltx-2.3-22b`（w7900d）
- **验收条件**:
  - 每种模态都有结果写入 `benchmark_results` 表
  - V14 新增的模态专有列（如 image_size、audio_duration）有值
  - 没有"chat requester 硬编码"式的 fallback warning
- **建议设备**: w7900d + aibook
- **证据**:
  - `artifacts/uat/v0.4/u11-aibook-w7900d/README.md`
  - `artifacts/uat/v0.4/u11-aibook-w7900d/01-aibook-ssh.txt`
  - `artifacts/uat/v0.4/u11-aibook-w7900d/02-w7900d-services.txt`
  - `artifacts/uat/v0.4/u11-aibook-w7900d/03-w7900d-http-probe.txt`
  - `artifacts/uat/v0.4/u11-aibook-w7900d/04-w7900d-t2v-server.py.txt`
  - `artifacts/uat/v0.4/u11-aibook-w7900d/05-gb10-4t-tts-asr-assets.txt`
  - 2026-04-20 本轮优先尝试不打扰共享服务的只读验证：直接复用 `w7900d` 现成 `ComfyUI:8188` 与 `t2v-server.py:8006`，同时复核 `aibook` 的 TTS/ASR 可达性，并用 `gb10-4T` 做本地 speech 资产 fallback 盘点
  - 结果 1：`aibook@100.106.164.54` 仍然 SSH timeout，原计划的 TTS/ASR 主机不可用
  - 结果 2：`gb10-4T` 的常见模型根目录里未发现 `mooer` / `funasr` / `qwen3-tts` / `qwen3-asr` 等本地 speech 资产，因此没有安全的替代主机可补 TTS/ASR
  - 结果 3：`w7900d` 上现成 T2I 服务是 `ComfyUI`，其 HTTP 面不符合当前 `image_gen` benchmark requester 契约：`GET /` 返回 UI HTML，`POST /v1/images/generations` 返回 `405`，而 fallback 形状 `POST /prompt` 返回 `500`
  - 结果 4：`w7900d` 上现成 T2V 服务 `t2v-server.py` 从源码看理论上兼容当前 `video_gen` requester（`GET /health`、`POST /`），但 live probe 下 `GET /health` 与 `POST /` 都在 15s 内超时；由于这是共享长跑服务，本轮未强制重启它
  - 结果 5：在当前实验室状态下，本项无法安全地产出五种模态的 `benchmark_results` 证据，也无法验证 V14 的模态专有字段落库；因此仍不满足验收条件

---

## U12 — MCP Tool 合并兼容性 (101→61)

- **状态**: PASS
- **触发上下文**: commit `c5159c1` 合并 45+ 工具，旧 agent/client 可能仍在调用老名字。
- **前置条件**: `aima serve` 运行
- **执行步骤**:
  1. 通过 MCP client 调用已废弃的工具名：`app.register`、`app.provision`、`app.list`、`power.mode`、`power.history`
  2. 观察响应
- **验收条件**:
  - 返回清晰的 "tool not found" JSON-RPC 错误（code `-32601`）
  - 不是 silent drop、不是 500、不是崩溃
- **建议设备**: dev-mac 本地跑，用 curl/jq 调试
- **证据**:
  - `artifacts/uat/v0.4/u12-dev-mac/README.md`
  - `artifacts/uat/v0.4/u12-dev-mac/01-app-register.json`
  - `artifacts/uat/v0.4/u12-dev-mac/02-app-provision.json`
  - `artifacts/uat/v0.4/u12-dev-mac/03-app-list.json`
  - `artifacts/uat/v0.4/u12-dev-mac/04-power-mode.json`
  - `artifacts/uat/v0.4/u12-dev-mac/05-power-history.json`
  - 2026-04-20 在 `dev-mac` 用隔离 `aima serve --mcp` (`127.0.0.1:9192/mcp`) 直接发送 5 个废弃工具名的 `tools/call`
  - 结果：五个请求全部返回 JSON-RPC `error.code=-32601`，`error.message` 分别为 `Tool not found: app.register`、`app.provision`、`app.list`、`power.mode`、`power.history`

---

# P2 — 发布后可补

## U13 — 全设备 Smoke 矩阵

- **状态**: KNOWN ISSUE
- **触发上下文**: CLAUDE.md "ALL COLLECT, THEN ANALYZE" 核心流程，发布前的最终 sanity check。
- **执行命令**（每台设备）:
  ```bash
  ./aima version
  ./aima hal detect
  ./aima engine list
  ./aima model list
  ./aima deploy list
  ./aima device status
  ```
- **验收条件**: 10 台可达设备（排除 hygon、qjq2）全部无崩溃；版本号显示 `v0.4-dev`（pre-tag smoke）或 `v0.4.0`（post-tag smoke）。Release 流程：先在 `develop` 验完 pre-tag smoke → 合 release/v0.4.0 → 打 tag → 重新 `make all` → 补一轮 post-tag smoke 验证 `v0.4.0` 字符串。原"必须显示 v0.4.0"的单点验收是 chicken-and-egg（UAT 在 tag 之前跑），已松绑为上述两阶段验证。
- **建议设备**: dev-mac、test-win、gb10、gb10-4t、linux-1、amd395、m1000、metax-n260、aibook、w7900d
- **证据**:
  - `artifacts/uat/v0.4/u13-matrix/README.md`
  - `artifacts/uat/v0.4/u13-matrix-current-head/README.md`
  - `artifacts/uat/v0.4/u13-post-tag/README.md`
  - `artifacts/uat/v0.4/u13-matrix/90-matrix-summary.md`
  - `artifacts/uat/v0.4/u13-matrix/dev-mac/`
  - `artifacts/uat/v0.4/u13-matrix/test-win/`
  - `artifacts/uat/v0.4/u13-matrix/gb10/`
  - `artifacts/uat/v0.4/u13-matrix/gb10-4t/`
  - `artifacts/uat/v0.4/u13-matrix/linux-1/`
  - `artifacts/uat/v0.4/u13-matrix/amd395/`
  - `artifacts/uat/v0.4/u13-matrix/w7900d/`
  - `artifacts/uat/v0.4/u13-matrix/aibook/`
  - `artifacts/uat/v0.4/u13-matrix/m1000/`
  - `artifacts/uat/v0.4/u13-matrix/metax-n260/`
  - `artifacts/uat/v0.4/u13-matrix-current-head/dev-mac/`
  - `artifacts/uat/v0.4/u13-matrix-current-head/gb10/smoke.txt`
  - `artifacts/uat/v0.4/u13-matrix-current-head/gb10-4t/smoke.txt`
  - `artifacts/uat/v0.4/u13-matrix-current-head/linux-1/smoke.txt`
  - `artifacts/uat/v0.4/u13-matrix-current-head/amd395/smoke.txt`
  - `artifacts/uat/v0.4/u13-matrix-current-head/w7900d/smoke.txt`
  - 2026-04-20 按 `CLAUDE.md` 的 smoke 命令集，对 10 台目标设备统一分发当前 `HEAD=44bc4c7e362d` 的 cross-built binary（`make all`，版本注入 `v0.4-dev`）后并行采集结果
  - 结果 1：7 台可达设备 `dev-mac`、`test-win`、`gb10`、`gb10-4t`、`linux-1`、`amd395`、`w7900d` 的 6 条 smoke 命令全部 `exit=0`，未出现 crash
  - 结果 2：3 台设备 `aibook`、`m1000`、`metax-n260` 在本轮 SSH 连接超时，按矩阵要求保留为 `UNREACHABLE`
  - 结果 3：所有可达设备的 `version` 输出一致为 `aima v0.4-dev`，这与当前 pre-tag smoke 规则一致，不再单独构成 blocker
  - 2026-04-21 又对当前 `HEAD=7dc8718` 做了一轮 reachable-only refresh：`dev-mac`、`gb10`、`gb10-4t`、`linux-1`、`amd395`、`w7900d` 的 smoke 命令集再次全部跑通，版本输出统一为 `aima v0.4-dev` / `commit 7dc8718`
  - 2026-04-21 在最终 `tag v0.4.0`（`master=c185d38`）上重新 `make all` 后补跑 post-tag smoke：`dev-mac`、`test-win`、`gb10`、`gb10-4t`、`linux-1`、`amd395`、`w7900d` 的 6 条 smoke 命令全部 `exit=0`
  - 结果 4：上述 7 台可达主机的 `version` 输出统一为 `aima v0.4.0` / `build 2026-04-21T11:14:24Z` / `commit c185d38`
  - 结果 5：`aibook`、`m1000`、`metax-n260` 在 post-tag 轮次仍然 SSH 超时，继续记为 `UNREACHABLE`
  - 结论：U13 仍不能记 `PASS`，但剩余缺口已经进一步收敛为 3 台设备不可达；`test-win` 的 current-head / post-tag refresh 已补齐，smoke 命令本身未见新的不稳定性

---

## U14 — Postgres CentralStore 契约一致性

- **状态**: KNOWN ISSUE（外部 Central 仓待同步）
- **触发上下文**: Central 已拆到 `aima-central-knowledge` 独立 repo，spec §4.2 要求 SQLite/Postgres 同套测试通过。
- **执行步骤**:
  1. 在 `aima-central-knowledge` repo 跑 `go test ./... -run TestCentralStore`，覆盖 SQLite 和 Postgres 两套实现
  2. 本 repo 侧只需验证客户端调用（`tooldeps_integration.go` 的 10 个出云 closure）对两种后端行为一致
- **验收条件**: 两套实现的契约测试全绿；本 repo 的 `central.sync/advise/scenario` MCP 工具在两种后端下响应结构一致
- **建议环境**: 生产 Central (`https://aimaservice.ai/central`) + 本地 SQLite Central
- **证据**:
  - `artifacts/uat/v0.4/u14-dev-mac/README.md`
  - `artifacts/uat/v0.4/u14-dev-mac/01-store-sqlite.txt`
  - `artifacts/uat/v0.4/u14-dev-mac/02-store-postgres.txt`
  - `artifacts/uat/v0.4/u14-dev-mac/10-central-sqlite.log`
  - `artifacts/uat/v0.4/u14-dev-mac/20-central-postgres.log`
  - `artifacts/uat/v0.4/u14-dev-mac/15-sqlite-advise.json`
  - `artifacts/uat/v0.4/u14-dev-mac/16-sqlite-scenario.json`
  - `artifacts/uat/v0.4/u14-dev-mac/17-sqlite-list-central.json`
  - `artifacts/uat/v0.4/u14-dev-mac/24-postgres-advise.json`
  - `artifacts/uat/v0.4/u14-dev-mac/25-postgres-scenario.json`
  - `artifacts/uat/v0.4/u14-dev-mac/26-postgres-list-central.json`
  - `artifacts/uat/v0.4/u14-dev-mac/27-backend-compare.txt`
  - `artifacts/uat/v0.4/u14-dev-mac/28-postgres-rfc3339-regression.txt`
  - 2026-04-20 在 `dev-mac` 本地用同一个 mock OpenAI 端点（`127.0.0.1:18082`）分别拉起 SQLite Central（`127.0.0.1:18083`）和 Postgres Central（`127.0.0.1:18084`），再用隔离 `AIMA_DATA_DIR` 实测 edge 侧 `knowledge sync --push --pull`、`knowledge advise`、`scenario generate`、`scenario list-central`
  - 结果 1：`aima-central-knowledge` repo 的 store 契约测试两套都通过。SQLite `TestSQLiteCentralStore` 与 Postgres `TestPostgresCentralStore` 都跑完同一套 `storeTestSuite`
  - 结果 2：edge 侧 `knowledge sync`、`knowledge advise`、`scenario generate` 在两种后端下保持同样的 edge-facing payload shape；差异只在预期的易变字段（`endpoint`、`device_id`、自动生成的 `id`/时间戳）
  - 结果 3：`scenario list-central` 的原始 JSON 仍有后端相关差异。SQLite 返回 RFC3339 UTC 时间戳（如 `2026-04-20T10:50:20Z`），Postgres 返回偏移时区格式（如 `2026-04-20 18:51:20+08`），因此“响应结构一致”这一验收条件未完全满足
  - 2026-04-21 本 repo 已在 `internal/central/store_postgres.go` 落地 RFC3339 UTC 归一化，并新增回归测试 `TestPostgresCentralStore_ListScenariosTimestampsRFC3339UTC`；`28-postgres-rfc3339-regression.txt` 为本地临时 Postgres 复测结果
  - 说明：由于生产/独立部署的 Central 代码仓已拆到 `aima-central-knowledge`，本项在那边同步同等补丁前，文档状态仍保留为待关闭

---

## U15 — Fleet mDNS 广播 + Exec

- **状态**: PASS
- **触发上下文**: commit `d58177e` "flip mDNS log filter from deny-list to allow-list"。
- **前置条件**: linux-1 + gb10-4t 在同一个局域网段
- **执行步骤**:
  1. 两台都 `aima serve`
  2. 任一台 `aima fleet info` → 应列出另一台
  3. `aima fleet exec --target <peer> -- aima hal detect`
- **验收条件**:
  - mDNS 发现成功
  - exec 返回对端正确的 HAL detect 输出
  - 日志没有 Docker veth / br-* 接口噪声
- **建议设备**: linux-1 + gb10-4t（如果在同网段）
- **证据**:
  - `artifacts/uat/v0.4/u15-gb10-4t/README.md`
  - `artifacts/uat/v0.4/u15-gb10-4t/00-serve.log`
  - `artifacts/uat/v0.4/u15-gb10-4t/01-fleet-devices.stderr`
  - `artifacts/uat/v0.4/u15-gb10-4t/01-fleet-devices.payload.json`
  - `artifacts/uat/v0.4/u15-gb10-4t/02-fleet-info-qujing24.stderr`
  - `artifacts/uat/v0.4/u15-gb10-4t/02-fleet-info-qujing24.payload.json`
  - `artifacts/uat/v0.4/u15-gb10-4t/03-fleet-exec-hardware-detect.stderr`
  - `artifacts/uat/v0.4/u15-gb10-4t/03-fleet-exec-hardware-detect.payload.json`
  - 2026-04-20 先只读确认网络前置：`gb10-4T` 为 `192.168.108.131/22`，`linux-1` 为 `192.168.109.23/22`，两台都在 `192.168.108.0/22` 局域网广播域内
  - 结果 1：在 `gb10-4T` 用当前 `v0.4-dev` 二进制起隔离 `serve --addr 0.0.0.0:6295 --mdns --allow-insecure-no-auth`，同时复用 `linux-1` 现有的 `aima-serve`（`6188`），未中断其长跑推理服务
  - 结果 2：从 `gb10-4T` 执行 `fleet devices` 可发现 peer `qujing24`（`192.168.109.23:6188`），说明 mDNS 广播与发现成功
  - 结果 3：`fleet info qujing24` 返回真实远端信息，包含 hostname `qujing24`、GPU `NVIDIA GeForce RTX 4090`、GPU count `2`
  - 结果 4：`fleet exec qujing24 hardware.detect` 返回对端 HAL detect payload，解析后同样可见 `NVIDIA GeForce RTX 4090` ×2、Ubuntu `22.04`
  - 结果 5：`gb10-4T` 的隔离 `serve.log` 只有正常 startup / mDNS broadcasting 行，没有 Docker `veth` / `br-*` 接口噪声，也没有 `mdns: unexpected log line`

---

## U16 — Overlay YAML Hardware Identity Granularity

- **状态**: KNOWN ISSUE（预期发布时留在 known issues 里）
- **触发上下文**: `docs/v0.4-explorer-bugs.md` O13 — `maybeCreateKnowledge` 使用 hwProfile 名（如 `nvidia-rtx4090-x86`）而不是 GPU arch（`Ada`）作为 `gpu_arch` 字段。overlay 只匹配该 profile，不能泛化到同架构其他设备。
- **当前行为**: 在发布文档里"Known Issues"段明确说明：自动生成的 overlay YAML 目前绑定到特定 hardware profile，不会自动泛化到同架构其他设备；v0.5 计划优化。
- **验收条件**: 不作为 UAT 跑，只需在 release notes 明确留言
- **证据**: 上游 bug 文档段 O13

---

# 证据管理

所有 UAT 执行证据请保存到：

```
artifacts/uat/v0.4/<uat-id>/
  ├── README.md          # 本次执行简报
  ├── serve.log          # aima serve 日志
  ├── aima.db.snapshot   # SQLite 快照（如涉及 DB 状态）
  ├── workspace/         # Explorer workspace 快照（如涉及 Explorer）
  └── screenshots/       # UI 测试截图/GIF
```

发布时 `release/v0.4.0` 分支需要保证 `artifacts/uat/v0.4/` 下至少有 P0 所有项的证据目录。

---

# 发布决策矩阵

| 前置条件 | 必须 | 建议 | 可选 |
|----------|------|------|------|
| P0 (U1-U5) | ✅ 全部 PASS | — | — |
| P1 (U6-U12) | ≥ 5/7 PASS | 全部 PASS | — |
| P2 (U13-U16) | — | U13 PASS | U14/U15 PASS |
| CHANGELOG 对齐 | ✅ | — | — |
| `series.txt = v0.4` | ✅ | — | — |
| 生产 Central 契约版本兼容 | ✅ | — | — |
| `design/v1.0-gap-analysis.md` 冻结 header | ✅ | — | — |

满足所有"必须"列即可打 `v0.4.0` annotated tag。

---

# 变更记录

| 日期 | 动作 | 作者 |
|------|------|------|
| 2026-04-20 | 初建文档 | Claude Code |
| 2026-04-21 | U5 / U2 patched rerun 收口，汇总更新为 9 PASS / 7 ISSUE | Claude Code |
| 2026-04-21 | U3 fast rerun 转 PASS；U1 增补 linux-1 复测并确认为环境/设备受限，汇总更新为 10 PASS / 6 ISSUE | Claude Code |
| 2026-04-21 | U1 的 `gb10-4T` rerun 转 PASS；current-head U13 smoke 补刷到 6 台可达主机，汇总更新为 11 PASS / 5 ISSUE | Claude Code |
| 2026-04-21 | `v0.4.0` tag 后 U13 smoke 补刷到 7 台可达主机（含 `test-win`）；版本统一为 `v0.4.0` / `c185d38`，汇总仍为 11 PASS / 5 ISSUE | Codex |
